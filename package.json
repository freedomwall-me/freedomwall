{
    "private": true,
    "scripts": {
        "format": "prettier --write app/**/*.php routes/**/*.php config/**/*.php"
    },
    "devDependencies": {
        "prettier": "^2.8.3",
        "@prettier/plugin-php": "^0.19.3"
    }
}
